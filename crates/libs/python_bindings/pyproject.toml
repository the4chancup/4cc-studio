[build-system]
requires = ["maturin>=1.15,<2"]
build-backend = "maturin"

[project]
name = "pes_models_native"
requires-python = ">=3.11"
dynamic = ["version"]

[tool.maturin]
module-name = "pes_models_native"
